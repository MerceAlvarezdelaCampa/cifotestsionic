import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jrbistuer.samplecifo',
  appName: 'samplecifo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
